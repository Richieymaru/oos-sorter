# GBU "GBU Soldout Sorter" app config (the NEW single-store, managed-install app).
# Deploy the notify extension to GBU with: shopify app deploy --config gbu --allow-updates
client_id = "c65e0cfd391decaeb61702cc7df311a3"
application_url = "https://sold-out-sorter.vercel.app"
embedded = true
name = "GBU Soldout Sorter"

[webhooks]
api_version = "2026-07"

[access_scopes]
scopes = "read_inventory,read_locations,read_products,write_products"
optional_scopes = [ ]
use_legacy_install_flow = false

[auth]
redirect_urls = [ "https://sold-out-sorter.vercel.app/api/oauth-callback" ]
